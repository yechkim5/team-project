package use_case;

import entity.Pokemon;

/** Use Move Use case:
 *
 * Actors: User, Pokemon, Move
 * Preconditions: User is in battle against another team of Pokemon
 *
 * Main Flow:
 * 1. User Opens attack menu for their active Pokemon
 * 2. User clicks on move that is currently usable (pp > 0)
 * 3. Attack display message is shown on screen
 * 4.
 *
 * 3. Determine if move will hit target Pokemon
 * 4.. If move will hit target calculate damage of
 */

public class useMove {
    Pokemon user;
    Pokemon target;


}
